allowBuilds:
  '@swc/core': false
  sharp: false
# Matches the supply-chain policy Vercel enforces on install (24h), so a
# dependency update that would be rejected there fails here instead.
minimumReleaseAge: 1440
